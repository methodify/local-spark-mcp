# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/

# uv
# (uv.lock IS committed)

# Spark
spark-warehouse/
metastore_db/
derby.log
*.crc

# Scala / sbt (token provider)
token-provider/target/
token-provider/project/target/
token-provider/project/project/

# Local config (the example is committed; real config may carry workspace specifics)
local-spark.local.toml

# OS / editor
.DS_Store
*.swp

# Windows zone identifier cruft
*:Zone.Identifier

/local-spark.toml
/.mcp.json
